class Solution {
    public int solution(String message) {
        int msg_lan = message.length();           
        int msg_letter = msg_lan * 2;
        
        int answer = msg_letter;       
        return answer;
    }
}